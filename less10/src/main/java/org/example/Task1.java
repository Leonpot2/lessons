package org.example;

public class Task1 {
    public boolean isEmailCorrect(String[] email) {
        if (!email.endsWith("@.ru") && !email.endsWith("@.com")) {
            return_false;
        }
        if (email.cointains("@")) {

        }

    return true;
    }
}